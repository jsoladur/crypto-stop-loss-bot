version: '3.9'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "8080:8000"
    env_file:
      - ./backend/.env
    networks:
      - appNetwork
networks:
  appNetwork:
    driver: bridge
volumes:
  postgres:
    driver: local